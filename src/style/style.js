import { createTheme } from "@mui/material";

export const writerAiTheme = createTheme({
  palette: {
    primary: {
      main: '#1b1b1b',
    },
    secondary: {
      main: "#FF6600",
    },
  },
});
