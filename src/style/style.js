import { createTheme } from "@mui/material";

export const writerAiTheme = createTheme({
  palette: {
    primary: {
      main: '#0a0908',
    },
    secondary: {
      main: "#FF6600",
    },
  },
});
